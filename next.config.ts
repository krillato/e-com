import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["www.google.com","ptcdn.info","via.placeholder.com", "images.unsplash.com", "loremflickr.com"],
  },
};

export default nextConfig;
